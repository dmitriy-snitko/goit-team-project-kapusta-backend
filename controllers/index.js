const { signUp, logIn, logout, userBalanceUpdate, getUserBalance, getCurrent } = require('./userControllers')

module.exports = {
  signUp,
  logIn,
  logout,
  userBalanceUpdate,
  getUserBalance,
  getCurrent
}
