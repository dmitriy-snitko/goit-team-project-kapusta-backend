const { signUp, logIn, logout, userBalanceUpdate, getUserBalance, getCurrent, getInfoForReports } = require('./userControllers')

module.exports = {
  signUp,
  logIn,
  logout,
  userBalanceUpdate,
  getUserBalance,
  getCurrent,
  getInfoForReports
}
